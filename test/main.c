extern void nas_print(char* msg);

int main()
{
    // Call the nas_print function with a message
    nas_print("Hello, world! Straight from C, linked with NAS assembly!");

    // Return success
    return 0;
}